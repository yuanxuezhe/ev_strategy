# evtrade 使用说明 (操作手册, 2026-09-09 统一 CPU/GPU 版)

> 项目根: `D:\workspace\ev_trd\` · 重构日期: 2026-09-09 · 适用代码: 重构后的 `evtrade/` 包
> 与本说明配套阅读:
> - **10-配置参数与运行指南.md** — 每个参数的工程含义 + FAQ
> - **12-重构与性能内核.md** — 包结构、vectorized 引擎、xp 算子、CPU/GPU 统一路径
> - **13-绩效评估与鲁棒选参框架.md** — WFO + 邻域衰减 + score + 置换检验
> - **14-统一策略契约.md** — `VectorizedStrategy` + `step(state,bar,params)` / `init_state` 主入口

---

## 0. 安装与前置

```bash
# 核心依赖 (torch 统一 CPU/GPU 后端; 无 numba)
pip install pymysql sqlalchemy pandas numpy torch
# GPU 回测: torch 需装 CUDA 版 wheel (cpu 版 wheel 只跑 CPU)
pip install torch --index-url https://download.pytorch.org/whl/cu124   # CUDA 12.4 示例
```

- 数据库: `192.168.10.2:33066` 的 `evtrade` 库, `minute_bars` 表
  (`stock_code, stime, open, high, low, close, volume`)
  - 凭据可被环境变量 `EVTRADE_DB_URL` 整体覆盖, 默认值含密码, **不要提交到公开仓库**
- 时间: `stime` 为 14 位 `YYYYMMDDHHmmss` 字符串; 字典序即时间序
- 任意目录运行命令即可

---

## 1. 子命令一览 (2026-09-09 收口)

```bash
python -m evtrade backtest [参数]   # 单次回测 (默认 vectorized / cpu)
python -m evtrade sweep    [参数]   # 网格并发扫描 + 鲁棒评分
python -m evtrade replay   [参数]   # 录制回放对账 (实盘一致性验收)
python -m evtrade params   [参数]   # 列出/检查策略参数 schema
```

```bash
# 全部测试
python -m pytest tests -q           # ~26 个 test 文件, CPU/GPU 容差 + 对账 + metrics

# 兼容旧入口 (过渡期)
python -m evtrade backtest --engine kernel ...   # 打 DeprecationWarning, 自动映射 --device auto
```

## 1.5 包结构 (2026-09-09 重构后)

```
evtrade/
├── __init__.py             顶层导出 (kernel stub 兼容旧 import)
├── __main__.py             python -m evtrade 入口
├── cli.py                  CLI 主入口 (backtest/sweep/replay/params 子命令)
│
├── feeds/                  行情入口
│   ├── base.py             Feed 抽象
│   ├── mysql_history.py    MySQLBacktestFeed (@register_feed)
│   ├── chained.py          ChainedFeed (@register_feed)
│   └── _registry.py        get_feed / register_feed
│
├── strategies/             策略目录 (单基类 VectorizedStrategy)
│   ├── vectorized_base.py  ★ VectorizedStrategy + 注册表 + params_spec + 展示 hook
│   ├── channel_deviation.py  @register_strategy (xp 算子 + Python FSM 共享)
│   └── ma_crossover.py       @register_strategy (纯数组算子示例)
│
├── indicators/             指标目录 (纯 Python + xp 算子)
│   ├── ema.py              xp_ema / xp_ema_channel / ema_push / ema_current / ema_channel_*
│   ├── atr.py              xp_atr / atr_push / atr_current / true_range
│   ├── rsi.py              Wilder RSI
│   └── boll.py             sma / bollinger
│
├── execution/              下单撮合
│   ├── base.py             Executor / SimulatedExecutor / BrokerExecutor
│   └── account.py          Account
│
├── core/                   主调度层
│   ├── config.py           全局常量
│   ├── timeutils.py        compute_bucket / bucket_ts_encoded / daterange
│   ├── aggregator.py       BarAggregator (桶合并, 供 Engine.on_bars 用)
│   ├── engine.py           ★ Engine (逐 bar 路径, 桶 CLOSE 语义)
│   ├── vectorized_engine.py  ★ run_vectorized (批量向量化路径, CPU/GPU 统一)
│   ├── metrics.py          bars_to_arrays / trades_to_list / summarize (30 字段, 含 x_mdd)
│   ├── data.py             MySQL 拉数 + npz 缓存 + 合成数据
│   ├── sweep.py            并发参数扫描 + WFO + score (kbs/13)
│   ├── replay.py           录制回放 + 对账 (vectorized vs Engine.on_bars)
│   ├── permutation.py      MC 置换检验 (kbs/13)
│   ├── gpu.py              CUDA 基础设施 (torch.cuda 探测 + 预计算缓存; 无 NVRTC 编译)
│   └── capability.py       GPU/CPU 能力探测
```

**顶层只剩 3 个文件**: `__init__.py` / `__main__.py` / `cli.py`。
**注册表使用模式** (沿用, 与旧版兼容):

```python
# 行情入口
from evtrade.feeds import get_feed, available_feeds
feed = get_feed("mysql_history", code="159992.SZ", start_ymd="20250101", end_ymd="20260903")
print(available_feeds())   # ['chained', 'mysql_history']

# 策略目录
from evtrade.strategies import get_strategy, available_strategies
s = get_strategy("channel_deviation", params={"low1": 1.5, "low2": 1.0})
print(available_strategies())   # ['channel_deviation', 'ma_crossover']

# 指标 (xp 算子, CPU/GPU 透明)
from evtrade.indicators import xp_ema, xp_ema_channel
my_emas = xp_ema_channel(xp, highs, lows, 21)
```

---

## 2. 策略参数通用 dict 模式 (2026-09-09)

### 2.1 三种调用形式 (等价)

策略参数统一以 `dict[str, Any]` 传递 (兼容 kwargs):

```python
from evtrade.strategies import get_strategy

# 形式 1: dict 传入 (推荐, 适合 grid 扫描)
s = get_strategy("channel_deviation", params={"low1": 1.5, "low2": 1.0})

# 形式 2: kwargs 传入 (适合单次回测)
s = get_strategy("channel_deviation", low1=1.5, low2=1.0)

# 形式 3: 混合 (dict 优先, kwargs 填默认)
s = get_strategy("channel_deviation", params={"low1": 2.0}, low2=1.5)
```

### 2.2 策略声明参数 schema (`params_spec`)

每个策略类声明 `params_spec`, 基类自动**校验类型 + 默认值 + 范围**:

```python
from evtrade.strategies import VectorizedStrategy, register_strategy

@register_strategy("my_strategy")
class MyStrategy(VectorizedStrategy):
    params_spec = {
        "lookback":      {"default": 20,    "type": int,   "min": 2,    "max": 1000},
        "threshold_pct": {"default": 0.001, "type": float, "min": 0.0,  "max": 0.1},
    }

    def __init__(self, params=None, **kwargs):
        super().__init__(params=params, **kwargs)
        # self.params["lookback"] / self.params["threshold_pct"] 已可用

    def init_state(self, params):
        return {"lookback_ema": None}

    def step(self, state, bar, params):
        # 主入口: bar = {ts, o, h, l, c, v, mark} 单桶
        # 返回 (new_state, sig); sig ∈ {-1, 0, 1}
        if bar["mark"] == 0:
            return state, 0
        state["lookback_ema"], ema = ema_step(state["lookback_ema"], bar["c"], int(params["lookback"]))
        sig = 1 if bar["c"] > ema else (-1 if bar["c"] < ema else 0)
        return state, sig
```

> 策略只写一个 `step`；state 由 engine 持有跨调用（推荐 `@dataclass`）。指标用
> `from evtrade.indicators import ema_step`。完整范式见 14 号文档 §2 / 11 号文档 §3。

校验失败时报 `ValueError` (未声明 key / 超范围) / `TypeError` (类型错)。

### 2.3 CLI 单次回测 (`--params`)

```bash
# 完整形式
python -m evtrade backtest --strategy channel_deviation \
    --params "low1:1.5;low2:1.0;high1:1.5;high2:0.5" \
    --period 5m --start 20250101 --end 20260903 --data-cache cache

# 部分覆盖 (其它走策略默认)
python -m evtrade backtest --strategy channel_deviation \
    --params "low1:2.0;low2:1.2" --device gpu --no-sleep
```

### 2.4 选 device

```bash
--device cpu    # torch CPU 算子
--device gpu    # torch CUDA 算子 (CUDA 不可用时自动 fallback cpu + warning)
--device auto   # 默认; gpu 可用则 gpu, 否则 cpu
```

> 旧 `--engine kernel / ref / vectorized` 已删除。检测到 `--engine` 时打 DeprecationWarning 后自动映射
> (`kernel → auto`, `ref / vectorized → cpu`), 不长期保留。

---

## 3. CLI 命令完整列表 (简表; 完整见 10 号文档)

### 3.1 backtest

| 参数 | 默认 | 说明 |
|---|---|---|
| `--strategy` | `channel_deviation` | 策略 key |
| `--device` | `auto` | cpu / gpu / auto |
| `--period` | `5m` | 任意 m/h/d |
| `--start` / `--end` | `20250101` / `20260903` | 策略起止 YYYYMMDD |
| `--code` | `159992.SZ` | 证券代码 |
| `--params` | (策略默认) | `k1:v1;k2:v2` |
| `--warmup-days` | `365` | 预热天数 |
| `--data-cache` | 无 | 行情 npz 缓存目录 |
| `--trade-qty` | `10000` | 单笔交易股数 |
| `--scale` | `1.0` | 倍投系数 (06 第 8 节) |
| `--no-sleep` | 关 | 去掉 0.1s 仿真延迟 |
| `--verbose` | 关 | 逐根打印策略 `format_signal_line` 输出 (sig!=0 时) |

> 策略私有参数 (如 `tf1`、`low1/low2/high1/high2`) 不再是独立 CLI 参数，
> 一律经 `--params "tf1:21;low1:1.5;..."` 传入 (按策略 `params_spec` 校验)。

### 3.2 sweep

```
--strategy <key>  --device {cpu,gpu,auto}
--grid "k=v1,v2"          # 可多次
--split <YYYYMMDD>        # 单窗 (兼容旧名)
--splits d1,d2,d3         # 滚动 WFO 多窗
--fee-bp 5                # 费率 bp
--score-lambda 1.0        # 稳健偏好旋钮
--min-trades 30           # 硬过滤
--max-mdd 1.0             # 硬过滤 (实盘建议 0.15)
--mc 300 --mc-top 5       # 蒙特卡洛置换检验
--workers 16              # 线程池 (CPU 路径)
--out results.csv         # 输出 CSV
--synthetic-days N        # 无库体验 (合成数据)
```

### 3.3 replay

```
--log <bars.csv>          # bar 日志 (stime,code,o,h,l,c,v)
--strategy <key>          # 策略 key
--device {cpu,gpu,auto}   # vectorized 路径
--warmup-until <YYYYMMDDHHMMSS>
--against-ref             # 同时用 Engine.on_bars 回放并对账 (vectorized vs Engine)
--signals-out <out.csv>   # 信号轨迹
```

---

## 4. 典型命令

```bash
# 标准 CPU 全速回测
python -m evtrade backtest --strategy channel_deviation --device cpu \
    --period 5m --start 20250101 --end 20260903 --no-sleep

# GPU 回测 (torch CUDA 不可用自动 fallback cpu + warning)
python -m evtrade backtest --strategy channel_deviation --device gpu \
    --period 5m --start 20250101 --end 20260903 --no-sleep

# 小段快速验证参数
python -m evtrade backtest --strategy channel_deviation \
    --params "tf1:21;low1:1.0" \
    --period 5m --start 20250601 --end 20250901 --no-sleep

# 换标的 + 1 小时周期 + 更苛刻的偏离阈值
python -m evtrade backtest --strategy channel_deviation --code 513120.SH \
    --period 1h --params "low1:2.0;low2:1.2;high1:2.0;high2:0.8" --no-sleep

# 带节奏的"仿真回放" (每根 1m bar 间隔 0.1s, 观察实时行为)
python -m evtrade backtest --strategy channel_deviation --period 5m \
    --start 20250601 --end 20250615

# 回放对账 (vectorized vs Engine.on_bars 逐桶 + 逐笔)
python -m evtrade replay --log /tmp/demo.log --strategy channel_deviation \
    --device cpu --against-ref

# 网格扫描 + 鲁棒评分
python -m evtrade sweep --strategy channel_deviation --device auto \
    --data-cache cache \
    --grid "low1=1.2,1.5,2.5" --grid "low2=0.3,0.5,0.8" \
    --grid "high1=1.2,1.5,2.5" --grid "high2=0.3,0.5,0.8" \
    --splits 20260101,20260401,20260701 \
    --fee-bp 5 --score-lambda 1.0 --min-trades 30 --max-mdd 0.15 \
    --mc 300 --mc-top 5 --out results_robust.csv

# 列出 / 校验策略参数 schema
python -m evtrade params --strategy channel_deviation --check
```

---

## 5. 输出解读 (简表; 完整见 10 号文档 §4)

### 5.1 分段进度 (Feed 打印)

```
  -- 段 20240102~20240108 处理 8231 根, 累计 8231
```

### 5.2 信号行 (Engine 打印, 仅 verbose + 有信号时)

```
BUY  >>> [20250106101500] | UP=1.2345 DW=1.2340 | low_dev(L/DW)=2.31% high_dev(H/UP)=0.45%
        >> BUY  10000股 @ 1.2345  花费 12345.00  剩余资金 187655.00 持仓 210000
```

### 5.3 盈亏汇总

**vectorized 路径** (30 字段):

```
============ 回测盈亏汇总 (vectorized / cpu) ============
final_cash / final_position / final_equity
n_trades / n_buy / n_sell / turnover
cagr / sharpe / sharpe_excess / sortino / sortino_excess / calmar
max_dd_days / max_dd_recovered / max_drawdown
```

**Engine 路径** (6 项基础汇总):

```
============ 回测盈亏汇总 (engine) ============
期末价 / 交易次数 / 期初资金·持仓 / 期末资金·持仓 / 期末持仓市值
策略总资产 / 不操作基线 / 盈亏差额(策略-基线) / 盈亏比例
```

---

## 6. FAQ

**Q: 完全没有信号?**
确认预热充分 (`--period 1d` 且 start 前历史不足一年会导致指标就绪晚); 观察偏离量分布
(把 `--low1` 临时调小如 0.5 看是否开始出信号), 区分阈值过严 vs 数据/代码问题。

**Q: 信号时间戳为什么是 10:15 而我以为是 10:10~10:14 的行情?**
桶 ts 是前开后闭区间的右端点, `[10:15]` 桶覆盖 `(10:10, 10:15]`。详见 04 文档。

**Q: 能回测多个标的吗?**
单次运行单标的 (Feed 按 `stock_code` 过滤)。多标的需循环调用或自行扩展 Feed (见 11 文档)。

**Q: 如何改为实盘?**
换 Feed (`ChainedFeed(历史, 实时)`) + 换 Executor (实现 `BrokerExecutor.trade` 接券商 API),
Engine/策略/账户不动。见 07、11 文档。

**Q: 中途 Ctrl+C 会丢汇总吗?**
不会。`run()` 捕获 KeyboardInterrupt 后仍执行 `flush()` + `_flush_final_bucket()`,
`main` 继续走 `print_summary()`, 基于已处理数据出汇总。

**Q: GPU 与 CPU 结果对不上?**
通常发生在 torch CUDA 不可用、auto 回退 cpu 时。显式传 `--device cpu` / `--device gpu` 排查;
`reconcile` 的 `bucket_diff_cap` 默认容忍 EMA 累积漂移 (≤8 桶), 超出需检查指标或策略代码。

**Q: 旧脚本里的 `--engine kernel/ref/vectorized` 还能用吗?**
会打 `DeprecationWarning` 并自动映射, 下个版本会删除该兼容项。建议新脚本直接用 `--device`。

---

## 7. 重构历史摘要 (与 12 号文档 §1 配套)

| 日期 | change / commit | 关键变更 |
|---|---|---|
| 2026-09-04 | (git 5585371) | 单文件 `mysql_analyze_demo.py` 685 行 |
| 2026-09-05 | 包拆分 | `evtrade/` 子包化 + BarAggregator + WFO 评分 |
| 2026-09-06 | DSL 步骤 1~2 | `_CALL_WHITELIST` + `kernel_dsl.py` 特化内核 |
| 2026-09-07 | DSL 步骤 3 | 三端单源 (Python / numba / CUDA) |
| 2026-09-08 | state_spec 工厂化 | kernel/gpu 按 state_spec 投影; 桶表去指标化; 策略展示 hook |
| 2026-09-09 | decouple-indicators | framework 完全不假定指标; `incremental_indicators.py` 删除 |
| 2026-09-09 | **unify-strategy-contract** | **删 DSL 渲染层 + numba 流式内核 + NVRTC CUDA 编译**; `VectorizedStrategy` 统一基类; CPU (numpy) / GPU (cupy) 双端统一; `pyproject.toml` 删 numba; CLI 收口 `--device {cpu,gpu,auto}`; 引擎改为桶 CLOSE 语义; `metrics.summarize` 补齐 16 字段; reconcile 对账默认 `bucket_diff_cap=8` (lenient) |
