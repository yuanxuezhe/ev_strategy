# 06 交易策略详解：通道偏离回撤

> 相关源码：`ChannelDeviationStrategy`（`evtrade/strategies/channel_deviation.py`），
> 统一策略契约 `VectorizedStrategy`（`evtrade/strategies/vectorized_base.py`）。
> 本文档为 2026-09-09 统一 CPU/GPU 重构版（DSL body 已废，全部 Python；CPU/GPU 由 torch 后端统一路由）。

## 1. 策略思想

均值回归型策略，使用 **EMA 通道**（UP=EMA(H, tf1)，DW=EMA(L, tf1)）作为偏离参考。
EMA 增量状态由策略 `step(state, bar, params)` 内部通过
`evtrade/indicators.ema_channel_step(state.ema, h, l, p)` 自维护
（`tf1` 由策略 `params_spec` 声明，默认 21）；
引擎 vectorized 路径逐桶调同一份 `step`（无独立 xp 批量 EMA）。

策略两段式判断：

- **第一段（极端偏离，置位标记）**：价格远跑到通道外 → 记下"发生过极端偏离"（锁存，不动作）
- **第二段（回撤确认，触发信号）**：随后价格收回通道附近 → 触发买入/卖出

| 信号 | 含义 | 逻辑链 |
|---|---|---|
| **BUY** | 超跌反弹 | L 远低于下轨（low_dev > low1）→ 之后 H 回升到下轨上方/附近（low_dev_h < low2）→ 买 |
| **SELL** | 超涨回落 | H 远高于上轨（high_dev > high1）→ 之后 L 回落到上轨下方/附近（high_dev_l < high2）→ 卖 |

两段式 + 锁存 = **迟滞（hysteresis）**：触发条件必须先"走出很远"再"收回来"，单边贴着阈值抖动不会连续打信号。

## 2. 四个偏离量（全部为百分比）

设当前周期桶 H=最高价、L=最低价，通道 UP、DW 由 `step(state, bar, params)` 内部调
`ema_channel_step(state.ema, h, l, p)` 算得; 引擎 vectorized 路径逐桶调同一份 `step`（无独立 xp 批量 EMA）:

| 变量 | 公式 | 直觉含义 | 用途 |
|---|---|---|---|
| `low_dev` | `(DW - L)/DW × 100` | L 低于下轨多少（>0 才在轨外） | 判定下轨**极端偏离** |
| `high_dev` | `(H - UP)/UP × 100` | H 高于上轨多少 | 判定上轨**极端偏离** |
| `low_dev_h` | `(DW - H)/DW × 100` | H 相对下轨的位置（H≥DW 时 ≤0） | BUY 触发：`< low2` 即 H 已收回到下轨上方或仅差一点 |
| `high_dev_l` | `(L - UP)/UP × 100` | L 相对上轨的位置（L≤UP 时 ≤0） | SELL 触发：`< high2` 即 L 已回落到上轨下方或仅差一点 |

注意方向：`low_dev_h < low2` 在 H 完全收回下轨上方时（负值）恒成立，所以该条件表达的是"整根 bar 的最高点已回到下轨附近以内"。

## 3. 默认参数

| 参数 | 默认 | CLI | 角色 |
|---|---|---|---|
| `tf1` | 21 | `--params "tf1:21"` | EMA 通道周期（策略私有，`step` 内调 `ema_channel_step`；引擎 vectorized 路径逐桶调同一份 `step`，无独立 xp 批量 EMA） |
| `low1` | 1.5% | `--params "low1:1.5"` | 下轨极端偏离阈值（置位） |
| `low2` | 1.0% | `--params "low2:1.0"` | 下轨回撤确认阈值（触发 BUY） |
| `high1` | 1.5% | `--params "high1:1.5"` | 上轨极端偏离阈值（置位） |
| `high2` | 0.5% | `--params "high2:0.5"` | 上轨回撤确认阈值（触发 SELL） |
| `init_cash` | 200000 | `--params "init_cash:200000"` | 期初资金（策略 state 自管，2026-09-13） |
| `init_position` | 0 | `--params "init_position:0"` | 期初持仓股数（策略 state 自管） |
| `trade_qty` | 10000 | `--params "trade_qty:10000"` | 每笔交易股数（策略 step 内撮合用） |
| `buy_pct` | 0.0 | `--params "buy_pct:0.5"` | BUY 按 cash 比例（0=关闭走 trade_qty） |
| `sell_pct` | 0.0 | `--params "sell_pct:0.5"` | SELL 按 position 比例（0=关闭走 trade_qty） |

> 参数经 `--params "tf1:21;low1:1.5;low2:1.0;high1:1.5;high2:0.5"` 一次传入（cli.py），
> 由 `_resolve_params` 按 `params_spec` 填默认 + 类型/范围校验（`vectorized_base.py`）；
> 未声明的参数名会报错（防拼写错误）。

**硬约束：必须 low1 > low2、high1 > high2**，否则"先冲出、再收回"的迟滞结构退化，会立即置位立即触发，锁存失去意义。

## 4. 状态机与锁存

每个方向两个状态位 + 一个单桶操作锁：

```
low_hit=False ──low_dev>low1──▶ low_hit=True   （置位，此后保持，哪怕偏离回落）
low_hit=True  ──low_dev_h<low2──▶ BUY, low_hit=False （触发并解除）
```

上轨对称：`high_dev>high1 → high_hit=True`；`high_hit 且 high_dev_l<high2 → SELL 并解除`。

要点（见 `channel_deviation.py::_fsm_step`）：

1. **置位后不会被清除**：即使 low_dev 回落到 low1 以下，low_hit 仍是 True，必须等到回撤确认（low_dev_h < low2）才触发并解除——这就是"锁存"。
2. **触发基于"上一根遗留的标记"**：`_fsm_step` 先用进入时的 low_hit/high_hit 判断触发，再处理置位。
3. **单桶单操作**（`lock_ts / low_acted / high_acted`，维护在 `state.fsm` dataclass）：
   - 桶 ts 变化时（`cur_ts != lock_ts`）重置两个操作锁
   - 同一根周期桶内，每个方向"置位"和"触发"合计**最多发生一次**（`not low_acted` 等守卫）
   - 目的：防止桶内 H/L 大幅波动导致"置位→触发→再置位→再触发"的连环信号
4. **触发互斥**：BUY/SELL 判断是 `if / elif`，同一桶最多出一个信号。

> 持久状态字段（`low_hit / high_hit / lock_ts / low_acted / high_acted`）由策略
> 在 init_state() 初始化为 dataclass 字段 (`state.fsm`); 不再需要 AST 白名单 + 三端投影。

## 5. step 的算法骨架（strategy-step-only, 2026-09-10）

> **2026-09-10 变化 (strategy-step-only)**: 唯一抽象方法收敛到 `step(state, bar, params) -> (state, sig)`;
> state 由 engine 持有 (dataclass), 策略**无 instance attr** (无 `self._fsm` 等)。
> 算法与执行模型彻底分层: 策略只写"算指标 + FSM"; 引擎决定批量/逐 bar、CPU/GPU、mark 过滤。

### 5.0 状态 dataclass（策略内）

```python
@dataclass
class ChannelDeviationState:
    ema: EMAChannelState              # 上/下轨 EMA state (in/out)
    fsm: DeviationFSM                 # FSM 锁存 state (low_hit / lock_ts / ...)
    prev_ts: int = 0
    cur_high: float = 0.0
    cur_low: float = 0.0
    has_prev: bool = False
```

### 5.1 单 step骨架 (engine 循环调用)

```python
@register_strategy("channel_deviation")
class ChannelDeviationStrategy(VectorizedStrategy):
    params_spec = {...}

    def init_state(self, params):
        return ChannelDeviationState()

    def step(self, state, bar, params):
        if bar["mark"] == 0:
            return state, 0                          # 预热段

        tf1 = int(params["tf1"])
        low1, low2, high1, high2 = ...               # 4 阈值解析

        # 1) EMA 通道 (stateful, 增量; 引擎逐桶调同一份 step)
        if state.has_prev and state.prev_ts != bar["ts"]:
            state.ema, _, _ = ema_channel_step(state.ema, state.cur_high, state.cur_low, tf1)
        state.prev_ts = bar["ts"]
        state.cur_high = bar["h"]; state.cur_low = bar["l"]
        state.has_prev = True
        state.ema, up, dw = ema_channel_step(state.ema, bar["h"], bar["l"], tf1)

        # 2) 偏离 (stateless 算)
        devs = _compute_devs(up, dw, bar["h"], bar["l"])

        # 3) FSM (stateful, 锁存)
        sig = _fsm_step(state.fsm, bar["ts"], devs,
                        float(params["low1"]), float(params["low2"]),
                        float(params["high1"]), float(params["high2"]))
        return state, sig
```

完整实现见 `evtrade/strategies/channel_deviation.py` (~150 行, 单一 `step` 方法)。
> `_compute_devs(up, dw, h, l) -> Deviations` 返回 `@dataclass Deviations(low, high, low_h, high_l)`；
> 通道未就绪 (up/dw == 0) 时全 0，FSM 不触发。

### 5.2 关键约束

- **mark=0 (预热)**: step 内部直接返 `(state, 0)`, 不产信号
- **state 跨调用持续**: engine 持有 state 引用 (@dataclass), 每次 step 传入传出
- **CPU/GPU 一致**: 引擎单一路径循环调 `step(state, bar, params)`，CPU/GPU 共用同一份策略代码，信号 bitwise 一致（torch 后端 `tensor.to(device)` 透明路由）

### 5.3 ma_crossover 范式（stateful EMA + 上一根 diff）

```python
@dataclass
class MACrossoverState:
    fast: EMAState
    slow: EMAState
    prev_diff: float = 0.0
    has_prev: bool = False

def step(self, state, bar, params):
    fast_p, slow_p = int(params["fast"]), int(params["slow"])
    close = float(bar["c"])

    state.fast, fast = ema_step(state.fast, close, fast_p)
    state.slow, slow = ema_step(state.slow, close, slow_p)

    if state.fast.count < fast_p:
        return state, 0                              # fast 未就绪

    diff = fast - slow
    sig = 0
    if state.has_prev:
        if diff > 0 and state.prev_diff <= 0: sig = 1
        elif diff < 0 and state.prev_diff >= 0: sig = -1
    state.prev_diff = diff
    state.has_prev = True
    return state, sig
```

### 5.4 规范接口 (VectorizedStrategy 基类)

`VectorizedStrategy` 基类（strategy-step-only）：

```python
class VectorizedStrategy:
    def init_state(self, params) -> state | None:
        return None                          # 无状态策略默认

    def step(self, state, bar, params) -> tuple[state, int]:
        raise NotImplementedError            # 子类必实现

    def format_signal_line(self, ts, sig, info=None) -> str:
        ...                                  # 展示 hook
```

子类实现 `step` 即可；stateful 策略覆写 `init_state` 返回 `@dataclass` 初值。

## 6. 信号在何时以什么价执行

- 触发时点：**桶 CLOSE**（Engine 看到 `cur.ts != last_cur.ts` 那一刻），用上一桶 finalized OHLCV 驱动策略。
- 执行价格：`prev["close"]` = 上一桶最终收盘价（即最近一根 1m bar 的 close）。批量 / 逐 bar 路径同口径。
- 回测中 `SimulatedExecutor` 以该价即时成交；对实盘意味着"信号一到就市价下单"的语义。

## 7. 调参直觉

- `low1/high1` 越大：越挑剔"极端"，信号更少更逆势
- `low2/high2` 越接近 `low1/high1`：迟滞带越窄，触发越快（但也更容易在弱反弹/回落中入场）
- `high2` 默认 0.5 比 `low2` 1.0 小：SELL 的确认更贴近上轨（回落幅度要求更小），体现多空不对称
- 改参数前建议先小段日期回测，看信号行里 `low_dev/high_dev` 的实际分布再定阈值

## 8. 策略展示 hook

框架层不再假定任何指标字段名; 信号行打印由策略覆写 `format_signal_line` 完成
(`VectorizedStrategy` 默认实现仅打 OHLCV + 信号；channel_deviation 自定义展示):

- **`format_signal_line(ts, sig, info) -> str`**：verbose 引擎的信号 bar 打印行。
  hook 签名**无 positional 指标**; 所需字段（up/dw、偏离值）从 `info` dict 自取。
  Engine 只负责 `print`，不假设 info 的键。

> **2026-09-09 变化**:
> - 删除 `get_extra_bucket_columns` / `get_extra_signal_columns` (旧 bucket_table / per-bar bundle)。
>   framework 输出列已简化为 OHLCV + sig (桶表) / `(stime, signal)` (信号轨迹)；
>   策略若需要展示列, 应在 `format_signal_line` 内一次性算 (verbose 引擎才打)。
> - 不再有 per-bar bundle 数组 (`bundle_per_bar` 已删); 策略需要展示列用 `format_signal_line` 的 info dict。

> **2026-09-13 约定** (`openspec/changes/enrich-signal-line-context`):
> 协作现场最高频需求 = "在什么价位 / 什么 K 线上触发了 BUY/SELL"。
> `filtered_mr` 与 `channel_deviation` 的 `format_signal_line` hook MUST 在一行内同时展示
> **方向 (BUY/SELL) + 价格 + 触发该 sig 的 K 线 OHLCV**。约定：
> - 策略 `step` **入口处**先写 `self._last_info = {"side": "", "price": float, "o/h/l/c/v": float}`
>   （确保所有 early-return 路径也覆盖）；step **末尾**把 `side` 覆写为 `BUY`/`SELL`/`""`。
> - hook 内 MUST 用 `info.get(...)`, 禁止 `info[k]` 下标, 防止 info 缺字段时 KeyError。
> - 当 `sig != 0` 时 hook 返回字符串 MUST 含方向词 + 价格 + `o=`/`h=`/`l=`/`c=` 4 个字段名。
> - `channel_deviation` 原有 `up`/`dw`/`low_dev`/`high_dev`/`cash`/`position` 字段向后兼容保留。
> - 锁定 spec: `R: Strategy signal line shows trigger bar context`；测试 `tests/test_format_signal_line_enrich.py`。

## 10. Filtered Mean-Reversion 策略 (`filtered_mr`)

> 2026-09-11 新增: `openspec/changes/filtered-mr-strategy`。

`channel_deviation` 是纯均值回归, 在单边暴涨暴跌中容易"接飞刀"——逆势开仓被趋势打爆。`filtered_mr` 在 channel_deviation 基础上叠加 **4 重过滤**, 搭建双重防护网：

| 过滤 | 作用 | 默认参数 |
|---|---|---|
| 大周期顺势 | 解决方向问题, 避免逆势 | `--higher-period 1h` + EMA 60 |
| ADX 趋势强度 | 解决环境问题, 避开单边 | `ADX(14) > 30` 暂停 |
| ATR 波动率 | 规避暴涨暴跌 | `ATR(14) > 1.5 × ATR_MA(20)` 暂停 |
| Close 确认 FSM | 避免盘中影线刺穿即开仓 | 上一桶触轨 + 本桶 close 回到带内 |

### 10.1 大周期顺势过滤

策略维护**大周期桶跟踪**（用 `core/timeutils.bucket_ts_encoded` 判定桶切换, 不用 BarAggregator 因为它依赖 `Bar.stime` 而 step 只给 int ts）：

- 当前 bar 的大周期桶 ts 变化 → finalize 旧桶（用旧桶 close 推进 `EMA(higher_ema_period)`）
- 大周期多头（last_close > higher_ema）：关闭上轨做空信号, 只允许下轨做多
- 大周期空头：关闭下轨做多信号, 只允许上轨做空

### 10.2 ADX / ATR 增量计算

两个都用 **Wilder smoothing**（标准 ADX/ATR 公式）：

```
TR_t = max(H - L, |H - prev_close|, |L - prev_close|)
ATR_t = ATR_{t-1} + (TR_t - ATR_{t-1}) / atr_period
up_move = H_t - prev_close
dn_move = prev_close - L_t
+DM_t = max(up_move, 0) if up_move > dn_move else 0
-DM_t = max(dn_move, 0) if dn_move > up_move else 0
+DM_smooth_t = +DM_smooth_{t-1} + (+DM_t - +DM_smooth_{t-1}) / adx_period
-DM_smooth_t = -DM_smooth_{t-1} + (-DM_t - -DM_smooth_{t-1}) / adx_period
TR_smooth_t  = TR_smooth_{t-1} + (TR_t - TR_smooth_{t-1}) / adx_period
+DI = 100 × +DM_smooth / TR_smooth
-DI = 100 × -DM_smooth / TR_smooth
DX = 100 × |+DI - -DI| / (+DI + -DI)            (DI 和 = 0 时 DX = 0)
ADX_t = ADX_{t-1} + (DX_t - ADX_{t-1}) / adx_period
```

注意：本实现用 **`H - prev_close` / `prev_close - L`**（即与 ATR 共享 `prev_close`），
与教材 Wilder 标准的 `H - H_{t-1}` / `L_{t-1} - L` 在量级上接近但定义不同 —— 这是
工程取舍（共用 prev_close 状态，避免多存一份历史价），过滤阈值需在选参阶段校准。
`ADX` 就绪需 `adx_count >= 2 * adx_period`（Wilder smoothing 标准）。

### 10.3 Close 确认 FSM

策略不维护 `self._xxx` 实例状态，而是用 `state.touched_lower_prev` / `state.touched_upper_prev` 标记上一桶是否触轨。
**两桶连续触轨 + 本桶 close 回到带内 + 大周期顺势过滤** 才出信号：

```
touched_lower_prev=True AND 本桶 low < lower AND 本桶 close > lower AND higher_trend >= 0 → BUY
touched_upper_prev=True AND 本桶 high > upper AND 本桶 close < upper AND higher_trend <= 0 → SELL
```

- `higher_trend ∈ {-1, 0, +1}`：大周期 `EMA(higher_ema_period)` 就绪后用末根 close 比较得出（`+1` 多头 / `-1` 空头）；
  EMA 未就绪时为 `0`，BUY/SELL 两侧条件均满足（"未就绪 = 不挡"）。
- 触轨 FSM 状态在策略 `step` 末尾更新（用本桶是否触轨覆盖 prev），供下一桶判断。

### 10.4 决策矩阵

| 市场环境 | ADX | 大周期 | 动作 |
|---|---|---|---|
| 盘整震荡 | ≤ 30 | 任意 | 全功能 (4 重过滤全过 → 出 close 确认信号) |
| 单边多头 | > 30 | 多 | 禁做空, 只允许低吸做多 |
| 单边空头 | > 30 | 空 | 禁做多, 只允许高抛做空 |
| 极端暴动 | 任意 | 任意 | ATR 异常放大 → 全停 |

### 10.5 params_spec

```python
"higher_period":     {"default": "1h", "type": str}            # 大周期 (字符串, 走 timeutils.resolve_period_seconds)
"higher_ema_period": {"default": 60,    "type": int,  "min": 2,  "max": 1000}  # 大周期 EMA 周期
"cur_ema_period":    {"default": 20,    "type": int,  "min": 2,  "max": 1000}  # 本周期 EMA 周期
"atr_period":        {"default": 14,    "type": int,  "min": 2,  "max": 1000}  # ATR Wilder 周期
"atr_ma_period":     {"default": 20,    "type": int,  "min": 2,  "max": 1000}  # ATR MA 周期
"atr_vol_mult":      {"default": 1.5,   "type": float, "min": 1.0, "max": 5.0}  # 波动率过滤乘数
"adx_period":        {"default": 14,    "type": int,  "min": 2,  "max": 1000}  # ADX Wilder 周期
"adx_threshold":     {"default": 30.0,  "type": float, "min": 10.0, "max": 100.0}  # ADX 强趋势阈值
"band_mult":         {"default": 1.8,   "type": float, "min": 0.5, "max": 10.0}  # 通道上下轨乘数 (cur_ema ± band_mult × ATR)
```

> 与 `channel_deviation` 不同：`filtered_mr` 在 `mark=0` 段仍累积 EMA/ATR/ADX
> 状态（与 `ma_crossover` 语义一致）；`channel_deviation` 因为桶首/桶末 mark 在
> 预热边界两侧不一致（vectorized 取首根、Engine 取末根），整段跳过 EMA 累积以避免
> 对账两侧 sig 漂移（详见 KB 09 §"bucket mark 取值"备注 + `tests/test_strategy_unified.py`）。

### 10.6 不实现 batched_step

跟 `channel_deviation` 一样, FSM latch + 大周期桶跟踪难向量化, **不实现 `batched_step`**。sweep 自动走 ThreadPool（spec Scenario "Hook default 未实现走 ThreadPool"）。
