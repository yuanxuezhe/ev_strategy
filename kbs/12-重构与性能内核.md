# 12 重构与性能内核 (2026-09-05 起; 2026-09-09 统一 CPU/GPU)

> 本次改造的完整记录: 拆包 → numba 流式内核 → 差分测试 → 并发扫描 → 基准实测 → **DSL 退场 + PyTorch 统一**。
> 原始单文件 `mysql_analyze_demo.py` 的实现已拆入 `evtrade/` 包; 兼容入口已移除, 统一用 `python -m evtrade`。
> 带行号的源码地图对应 git 历史中的原始版本 (commit 5585371)。
>
> **2026-09-08 同步** (change `state-spec 工厂化`): kernel/gpu 按策略状态 spec 工厂化;
> DSL→CUDA 投影函数从 GPU 探测模块迁至 `strategies/dsl.py`;
> `bucket_table` 去指标化 + `bundle_per_bar` per-bar dict 契约。
>
> **2026-09-09 同步** (change `decouple-indicators-from-framework`):
> framework 完全不假定任何指标; `core/incremental_indicators.py` 整文件已删除;
> 指标由策略在 `step(state, bar, params)` 内部调 `evtrade/indicators/*.step(...)` 自维护。
>
> **2026-09-09 同步** (change `unify-strategy-contract`):
> **DSL 渲染层 + numba 流式内核 + NVRTC CUDA 编译整体下线**;
> 策略唯一入口 `step(state, bar, params) -> (state, sig)` (后续 strategy-step-only 落地);
> 性能内核由 PyTorch (CPU) / PyTorch CUDA (GPU) 承担 (2026-09-10 pytorch-unified-strategy)。
> `core/kernel.py` / `core/kernel_dsl.py` 及 GPU 模块中的通用 CUDA 扫窗函数已物理删除。
> `pyproject.toml` 删 `numba` 依赖; 删 `strategies/{base,dsl,example_*}.py` 共 4 文件。
>
> **2026-09-10 同步** (change `strategy-step-only`):
> 唯一抽象方法收敛到 `step(state, bar, params) -> (state, sig)`;
> 状态由 engine 持有 (`@dataclass`), 策略**无 instance attr** (无 `self._fsm` 等);
> engine 循环调 step, 批量 / 逐 bar 两条路径走同一份算法 (信号 bitwise 一致);
> 指标 `xxx_push / xxx_current` 6 标量 / 3 标量 API 统一为 `xxx_step(state, ...) -> (state, out)` 单函数 + dataclass。
>
> **2026-09-10 同步** (change `pytorch-unified-strategy`):
> 后端从 cupy/numpy 双端统一为 **PyTorch** 单端 —— `backends.get_xp(device) -> torch.device`
> (cpu/cuda), 策略代码一份跑 CPU/GPU; `pyproject.toml` 删 `cupy`/`numba`, 声明 `torch>=2.0`;
> GPU 环境探测改 `torch.cuda`; 新增 `evtrade/backends.py` 后端选择器。
>
> **2026-09-10 同步** (change `consolidate-simplify-core`): 删行情源子包 (数据加载内联
> `core/data.py`: `load_bars` / `synthetic_bars`)、删 atr/rsi/boll 指标 (仅留 EMA)、
> GPU 探测模块收敛为 `core/tsbucket.py` (precompute_ts_mark + LRU 缓存)、
> 删能力探测模块 (并入 `backends.resolve_device`)、删券商执行器占位
> (成交决策收口 `trade_decision`)、删 `--engine`/sleep 关闭/分段步长等 CLI 死 flag、
> CLI 共享父 parser 去重。

## 1. 包结构 (2026-09-09 重构后)

```
evtrade/
├── primitives.py   Bar / fmt
├── backends.py     ★ get_xp(device) -> torch.device; resolve_device / gpu_available; to_tensor / to_host (CPU/GPU 统一后端)
├── cli.py          python -m evtrade backtest | sweep | replay | params (--device {cpu, gpu, auto})
├── core/
│   ├── config.py        常量 (DB_URL 支持环境变量 EVTRADE_DB_URL 覆盖) / TABLE / 默认资金
│   ├── timeutils.py     compute_bucket_general / bucket_ts_encoded / resolve_period_seconds / 历法
│   ├── aggregator.py    BarAggregator
│   ├── engine.py        Engine (verbose 逐 bar 路径; 桶 CLOSE 语义)
│   ├── vectorized_engine.py  ★ run_vectorized 批量向量化路径 (torch 化; equity_curve 累积)
│   ├── data.py          load_bars (MySQL 一次拉取 + npz 缓存) + synthetic_bars 合成数据生成器
│   ├── metrics.py       ★ bars_to_arrays / summarize (~30 字段, 含 x_mdd)
│   ├── sweep.py         并发参数扫描 + walk-forward (全 vectorized 路径)
│   ├── replay.py        行情回放 + 对账 (vectorized vs Engine.on_bars)
│   ├── permutation.py   蒙特卡洛置换检验 (13 号文档)
│   ├── tsbucket.py      向量化桶 ts/mark 预计算 (precompute_ts_mark) + LRU 缓存
│   └── _harness.py      ListBarFeed (测试/回测用 bar 流 adapter)
├── indicators/          ema 仅 (纯 Python ema_step/ema_channel_step 增量 + ema/ema_channel numpy 批量; 无 atr/rsi/boll, 无 @njit)
├── strategies/
│   ├── vectorized_base.py  ★ VectorizedStrategy (唯一基类) + 注册表 + params_spec + step/init_state + 展示 hook
│   ├── channel_deviation.py  ChannelDeviationStrategy (*_step 增量 + Python FSM)
│   └── ma_crossover.py       MACrossoverStrategy (纯数组算子示例)
└── execution/        account.py (Account) / base.py (trade_decision + Executor + SimulatedExecutor)

tests/              test_strategy_unified / test_strategy_class_access /
                    test_strategy_template / test_metrics_v3 / test_metrics_units /
                    test_trade_unified / test_harness / test_sweep_window /
                    test_vectorized / test_cli / test_cli_params /
                    test_tsbucket_cache / test_torch_backend / test_timeutils /
                    test_backtest_defaults / test_defaults_loader /
                    test_sweep_save_defaults / test_pyproject / test_dead_code_removed
```

## 2. 性能内核: CPU 向量化 vs GPU 向量化 (2026-09-09 重写)

### 2.1 核心思想: 双端统一向量化

不再有"流式内核 + DSL 三端转译"。唯一执行入口是 `run_vectorized(bars_1m, period, warmup_until, strategy, params, device, ...)`:

| 场景 | 用法 | 特点 |
|---|---|---|
| 回测 (CPU) | `run_vectorized(..., device="cpu")` | torch CPU; `tensor.to("cpu")` 路由 |
| 回测 (GPU) | `run_vectorized(..., device="gpu")` | torch CUDA; 同样代码透明换 device |
| 扫描 | 每组参数一个 `run_vectorized`; ThreadPoolExecutor 并行 | CPU 多线程; GPU 路径建议单线程 (GPU 占用) |
| 实盘 | `Engine.on_bars` 逐 bar 调 `step(state, bar, params)` | 同 vectorized 路径共享 step 算法 |

### 2.2 实现要点

- **桶聚合** = `_aggregate_buckets_xp(xp, bars_1m, period_seconds, warmup_until)`:
  用 `xp.diff / xp.cumsum / searchsorted` 把 1m bar 数组聚合为桶级 OHLCV 数组。
  桶 ts / mark 用 `timeutils.bucket_ts_encoded` 整数化历法 (避免 GPU 上 int64 除法)。
- **桶级数组契约**: `{"ts", "o", "h", "l", "c", "v", "mark", "n_bars"}` — 全 1D 数组。
- **浮点路径**: PyTorch 的 GPU 算子舍入与 CPU 不同, **不保证 bitwise**;
  但策略逻辑确定性 + 同输入, 结果在 ULP 级容差内 (`test_ma_crossover_cpu_vs_gpu` 锁定)。
- **桶 CLOSE 语义**: 向量化路径在"桶级 finalized OHLCV"上算指标;
  `Engine.on_bars` 在桶**切换**那一刻用上一桶 finalized OHLCV 驱动策略 (与 vectorized 同口径)。
  `reconcile` 测试通过 `bucket_diff_cap=8` 容忍 EMA 累积顺序漂移。
- **指标未就绪**: 指标 `*_step` 在 state 未就绪 (seed 未满) 时返回 0 占位; 策略代码 `if up == 0.0 or dw == 0.0: return 0` 守卫 (两条路径同口径);
  vectorized 路径靠 `ema_channel_step` 的 warmup 返回 0, 等价于 Engine 路径的 0 守卫。

### 2.3 持久状态 (strategy-step-only)

| 路径 | 状态存放 |
|---|---|
| vectorized (`VectorizedEngine._compute_signals`) | engine 持有 state (dataclass 实例), 跨调用持续 |
| Engine (`Engine.on_bars`) | `Engine._state` instance attr (dataclass), 跨调用持续 |

**两种入口调同一份 `strategy.step(state, bar, params)`, state 由 engine 持有, 策略无 instance attr**。
channel_deviation 用 `ChannelDeviationState` dataclass; 两种入口信号 bitwise 一致 (`reconcile` 锁定)。

### 2.4 metrics 30 字段 (2026-09-09 补齐, 2026-09-10 扩到 30)

`vectorized_engine._execute_trades` 累积 `equity_curve / baseline_curve` (O(n));
末尾调 `metrics.summarize(equity_curve, baseline_curve, ..., years, n_trades, n_buy, n_sell, turnover, ...)`:

| 字段 | 含义 |
|---|---|
| `final_cash / final_position / final_equity` | 终态 |
| `n_trades / n_buy / n_sell / turnover` | 成交统计 |
| `cagr` | 年化超额 (final/baseline 比值年化) |
| `sharpe / sharpe_excess` | 日频收益 + 超额曲线的夏普 |
| `sortino / sortino_excess` | 下半方差 Sharpe |
| `calmar` | cagr / max_drawdown |
| `max_dd_days` | 最大回撤持续天数 |
| `max_dd_recovered` | 最大回撤恢复天数 |
| `max_drawdown` | 最大回撤幅度 |

## 3. 等价性保证 (差分 / 对账测试)

`tests/test_strategy_unified.py` + `tests/test_replay_v2.py` + `tests/test_vectorized.py`:

1. **CPU vs GPU 容差**: 同一策略同参数, `np.array_equal(sig_cpu, sig_gpu)`, summary 浮点字段 1e-6 容差 (`test_ma_crossover_cpu_vs_gpu`)。
2. **vectorized vs Engine 桶级对账**: `reconcile(...)` 比逐桶 sig + 逐笔成交 (ts/side/qty/price), 默认 `bucket_diff_cap=8`, strict 模式 cap=0。
3. **MA 交叉语义**: numpy 独立算 EMA + 交叉, 与 vectorized 引擎输出对比 (`test_vectorized_ma_crossover_logic`)。
4. **ma_crossover vs channel_deviation**: 两个策略在 CPU/GPU/vectorized/Engine 四条路径上的 6 项核心断言 (signature / dtype / cpu-vs-gpu / vectorized-vs-Engine / metrics 字段集)。

## 4. 并发扫描 (sweep.py + cli)

```bash
# 真实数据 (MySQL 自动拉取并缓存 npz, 二次运行零数据库开销)
python -m evtrade sweep --strategy channel_deviation --code 159992.SZ \
    --start 20250101 --end 20260903 --device auto \
    --data-cache cache \
    --grid "low1=1.0,1.5,2.0" --grid "high2=0.3,0.5,0.8" \
    --split 20250701 --workers 16 --out results.csv

# 无库体验 (合成数据)
python -m evtrade sweep --strategy channel_deviation --synthetic-days 120 \
    --start 20250601 --end 20250701 --device cpu \
    --grid "low1=0.4,0.6,0.8"

# GPU 档 (需 torch CUDA 环境)
python -m evtrade sweep --strategy channel_deviation ... --device gpu
```

- 每组参数 = 一次 `run_vectorized`, ThreadPoolExecutor 并行。
- `--device cpu` 多线程吃满; `--device gpu` 单线程 (GPU 占用)。
- `--split YYYYMMDD` 输出 train_*/test_ 两套指标; 按 score 排序 (见 13 文档)。
- 网格键: low1/low2/high1/high2/period/trade_qty/scale (策略私有参数如 `tf1` 在 `params_spec` 声明后透传扫描)。

## 5. 性能特性 (2026-09-09 重构后)

旧版 numba 加速特性 (`tests/test_kernel_unit.py::test_live_step_equals_batch` 期望微秒级 step) 已作废——本版无 JIT 内核。
- **速度特性**:
- torch CPU 路径: 批量向量化吞吐 ≈ 0.05~0.2 M bar/s (取决于策略复杂度, channel_deviation 含 FSM Python 循环较慢);
- torch CUDA 路径: 桶聚合走 torch 算子; 主要优势在大网格并发 (单 GPU batch);
- 实盘路径 (`Engine.on_bars`): 逐 bar Python 调用 + dataclass state 维护, 吞吐 ≈ 1~5 K bar/s, 满足实盘 1m bar 节奏。

> 旧 numba 基准 (168K bar / 9 ms) 已不适用; 本版无 JIT 内核, 吞吐以 torch 算子为准。

## 6. GPU 档 (2026-09-09 简化)

> **2026-09-09 简化**: 删除 GPU 模块中的 `cuda_sweep_window_generic` (NVRTC 编译 + 通用 CUDA kernel);
> 删 `strategies/dsl.py` (含所有 CUDA 投影函数); 删 GPU 模块中所有 `CUDA_DEVICE_*` 字符串常量。
> (该模块 2026-09-10 收敛为 `core/tsbucket.py`, 仅保留桶 ts/mark 预计算 + LRU 缓存。)
>
> GPU 路径现只通过 torch 算子 (`tensor.to("cuda")`) 自动获得, 无需手写 CUDA C99。

- **栈**: 消费级 NVIDIA GPU + `torch` (CUDA runtime 版 wheel)。
- **设计**: torch 算子 (cumsum / where / unfold 等) 在 CUDA 后端映射到预编译 kernel。
  桶 ts/mark 用整数化历法预计算 (numpy 向量化), 避免 GPU 上 int64 除法。
- **何时用 GPU**: 与 CPU 路径吞吐差距不大; 主要价值在大网格并发 (单 GPU 一次处理数千组参数)。
- **失败回退**: torch CUDA 不可用时 `--device auto` 自动 fallback cpu 并打 warning。

## 7. CLI 速查

```bash
python -m evtrade backtest --strategy channel_deviation --device cpu \
    --period 5m --start 20250101 --end 20260903

python -m evtrade backtest --strategy channel_deviation --device gpu \
    --code 159992.SZ --start 20250601 --end 20250630 \
    --warmup-days 200 --data-cache cache

python -m evtrade sweep --strategy channel_deviation --device auto \
    --grid "low1=1.0,1.5" --grid "high2=0.5,0.8" ...

python -m evtrade replay --log /tmp/demo.log --strategy channel_deviation \
    --device cpu --against-ref        # vectorized vs Engine 对账

python -m pytest tests -q               # 全部测试 (含差分 + GPU 容差 + 对账)
```
